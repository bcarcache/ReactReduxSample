import React, { Component } from 'react'

export default class RegistrySaveButton extends Component {
  render() {
    return (
      <center><input key={'btnSave'} type='submit' value='Save' className='btn btn-success'/></center>
    )
  }
}
